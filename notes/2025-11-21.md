## TypeScript tsconfig

- tsconfig.jsonのpathsはTypeScriptのコンパイル時にしか効かない
- 実行時にエイリアスを使うには、tsconfig-pathsをts-nodeと一緒に使う
- npx ts-node -r tsconfig-paths/register src/main.tsが魔法の呪文!

## TypeScript command line args

- npx ts-node main.ts foo bar baz
