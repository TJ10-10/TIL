# 2025-11-05: TypeScript Hello World

## 学んだこと
- `console.log("Hello World!")` が正しい（`console()` はエラー）
- `tsc hello.ts` で `.js` に変換
- `node hello.js` で実行

## 気づき
- `.ts` → `.js` の変換は明示的に行う
- `tsc` のエラーは型や構文のヒントになる

