## TILのまとめ

# Github push(初回)
    - git init
    - git add README.md
    - git commit -m "first commit"
    - git branch -M master
    - git remote add origin https://github.com/TJ10-10/til.git
    - git push -u origin master

# Git push(二回目以降)
    - git add       # pushしたいものを選択
    - git commit -m "message"
    - git push -u origin master

# TIL用Markdownファイルの作成
    - mkdir -p ../notes
    - nvim ../notes/2025-11-05.md

